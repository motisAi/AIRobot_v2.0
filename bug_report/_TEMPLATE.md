# Bug #NNN — <one-line title>

- **Date found:** YYYY-MM-DD
- **Status:** fixed | fixed-needs-verify | recurring
- **Area:** audio | vision | ai | conversation | network | hardware | power | deploy | face-ui | smart-home
- **Files touched:** `path/one.py`, `path/two.py`
- **Commit(s):** abc1234 (or "uncommitted" / "n/a")

## Symptom
What Moti (the user) saw or heard. Plain words.

## Root cause
The real cause, precisely.

## Fix
Exactly what was changed / which command fixes it. Include shell commands in fenced code blocks when relevant.

## How to verify
Concrete check(s) that prove it is fixed.

## Will it come back?
Yes/no and the trigger (e.g. "yes — every kernel upgrade; run `sudo dkms autoinstall`").
